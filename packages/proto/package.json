{
  "name": "@chefhub/proto",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Protocol Buffer definitions for ChefHub microservices",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm generate && tsc",
    "generate": "buf generate",
    "lint": "buf lint",
    "clean": "rm -rf dist generated"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.47.0",
    "@bufbuild/protobuf": "^2.2.0",
    "@bufbuild/protoc-gen-es": "^2.2.0",
    "typescript": "^5.9.3"
  }
}
