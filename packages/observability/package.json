{
  "name": "@chefhub/observability",
  "version": "1.0.0",
  "private": true,
  "description": "Shared observability utilities (logging, tracing, metrics)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "pino": "^9.0.0",
    "prom-client": "^15.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
